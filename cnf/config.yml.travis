drupal:
    settings:
        databases:
            default:
                default:
                    database: adonis
                    username: travis
                    password:
                    driver: mysql
                    host: localhost
    include:
        require_once: [%DRUPAL_ROOT . '/sites/default/vendor/autoload.php']
